import { useEffect, useState } from "react";
const EVENT_TIME=new Date("2026-10-17T06:30:00+08:00").getTime();
function remaining(){const distance=Math.max(0,EVENT_TIME-Date.now());return{days:Math.floor(distance/86400000),hours:Math.floor(distance/3600000)%24,minutes:Math.floor(distance/60000)%60,seconds:Math.floor(distance/1000)%60}}
export default function Countdown(){const [time,setTime]=useState(remaining());useEffect(()=>{const timer=window.setInterval(()=>setTime(remaining()),1000);return()=>window.clearInterval(timer)},[]);return <section className="bg-cyan-400 px-6 py-14 text-slate-950"><div className="mx-auto max-w-5xl text-center"><p className="font-black uppercase tracking-[.25em]">17 October 2026 · UMT</p><h2 className="mt-3 text-3xl font-black sm:text-5xl">Countdown to Race Day</h2><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-4">{Object.entries(time).map(([label,value])=><div key={label} className="rounded-2xl bg-slate-950 p-5 text-white shadow-lg"><p className="text-4xl font-black sm:text-5xl">{String(value).padStart(2,"0")}</p><p className="mt-2 text-xs font-bold uppercase tracking-widest text-cyan-300">{label}</p></div>)}</div></div></section>}
